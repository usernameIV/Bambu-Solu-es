/*import { Stack, useLocalSearchParams } from "expo-router";
import { View, Text } from "react-native";

const DetailsPage = () => {
    const {idProjeto} = useLocalSearchParams();

    return (
        <View>
            <Stack.Screen options={{headerTitle: `${idProjeto}`}}/>
            <Text>My Details for: {idProjeto}</Text>
        </View>
    );
};

export default DetailsPage; */